# TODO - Fix background.png not applied

- [ ] Inspect current background configuration (CSS + HTML body classes) and identify conflicts.
- [x] Update `index.html` body classes so `src/index.css` background image rule can apply cleanly.
- [ ] (If needed) Verify image URL is correct by checking `background-image: url('/background.png')` behavior with Vite base.
- [x] Run dev/build to confirm background loads.

